defmodule ElixirPassphraseDicewareGeneratorTest do
  use ExUnit.Case
  doctest ElixirPassphraseDicewareGenerator

  test "greets the world" do
    assert ElixirPassphraseDicewareGenerator.hello() == :world
  end
end
